# Example: Pythinker CLI Wire Messages

This example demonstrates how to create and run a Pythinker CLI instance with raw Wire message output.

```sh
cd examples/pythinker-code-wire-messages
uv sync --reinstall
uv run main.py
```
