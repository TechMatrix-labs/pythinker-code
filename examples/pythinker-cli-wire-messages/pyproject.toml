[project]
name = "pythinker-code-wire-messages"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pythinker-code",
    "rich",
]

[tool.uv.sources]
pythinker-code = { path = "../../" }
