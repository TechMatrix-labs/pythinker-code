# Example: Custom Pythinker Soul

This example demonstrates how to extend the `PythinkerSoul` (builtin agent loop) to customize its behavior and use it with the `Shell` UI.

```sh
cd examples/custom-pythinker-soul
uv sync --reinstall
uv run main.py
```
