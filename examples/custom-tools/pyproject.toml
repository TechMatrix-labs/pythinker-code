[project]
name = "custom-tools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["pythinker-code", "pythinker-core"]

[tool.uv.sources]
pythinker-code = { path = "../../" }
