# Example: Pythinker CLI Stream JSON

This example demonstrates how to run Pythinker CLI in a subprocess and interact with it using JSON messages over standard input and output.

```sh
cd examples/pythinker-code-stream-json
uv run main.py
```
