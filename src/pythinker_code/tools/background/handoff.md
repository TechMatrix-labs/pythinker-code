Return handoff information for a background task so a human can take over monitoring or stopping it.

This does not reattach the live process to the terminal. For shell tasks, use the output_path to inspect logs and TaskStop to cancel the task if needed.
